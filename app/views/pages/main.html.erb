

<br/>


  <% if current_user %>

    <%= render :partial => "users/show_user", :locals => {:user => current_user} %>

  <% else %>
    <div class="center hero-unit">
      <h1>Welcome! </h1>
      <br/>
      <%= form_tag("/auth/twitter?x_auth_access_type=read") do %>
      <%= button_tag "Sign in with Twitter", :class => "btn btn-primary" %>
      <% end %>
    </div>
  <% end %>

